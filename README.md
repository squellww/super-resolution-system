# 超高分辨率图像生成系统

<p align="center">
  <img src="https://img.shields.io/badge/Python-3.10+-blue.svg" alt="Python 3.10+">
  <img src="https://img.shields.io/badge/License-MIT-green.svg" alt="License: MIT">
  <img src="https://img.shields.io/badge/Streamlit-1.28+-red.svg" alt="Streamlit">
</p>

## 📋 项目概述

本项目实现了一个从720p输入到**1亿-2亿像素**输出的超高分辨率图像生成系统，采用**分块-并行-融合**的分布式架构，基于字节跳动Seedream 4.0和火山引擎技术栈。

### 核心特性

- 🎯 **超高分辨率输出**: 支持100MP/150MP/200MP像素级别输出
- 🚀 **并行处理**: Kimi Agent集群调度，60-120并发
- 🧩 **智能分块**: 内容感知分块，保护人脸/文字/产品主体
- 🔗 **无缝融合**: 拉普拉斯金字塔融合，消除接缝
- 📊 **质量评估**: PSNR/SSIM/LPIPS多维度质量验证
- 🖥️ **Web界面**: Streamlit交互界面，实时监控

## 🏗️ 系统架构

```
┌─────────────────────────────────────────────────────────────┐
│                    超高分辨率图像生成系统                      │
├─────────────────────────────────────────────────────────────┤
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐   │
│  │ 图像分块  │→│ 并行超分  │→│ 智能融合  │→│ 质量评估  │   │
│  │  Module  │  │  Module  │  │  Module  │  │  Module  │   │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘   │
│       ↑            ↑            ↑            ↑              │
│  ┌─────────────────────────────────────────────────────┐   │
│  │              Kimi Agent集群调度器                     │   │
│  │         (任务分发/负载均衡/故障恢复)                   │   │
│  └─────────────────────────────────────────────────────┘   │
│                        ↑                                    │
│  ┌─────────────────────────────────────────────────────┐   │
│  │              Streamlit WebUI 界面                   │   │
│  │    (上传/配置/监控/结果展示/批量处理)                  │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

## 📁 项目结构

```
super_resolution_system/
├── main.py                          # 主入口程序
├── app.py                           # Streamlit WebUI
├── config.py                        # 系统配置
├── requirements.txt                 # 依赖列表
│
├── tiling_module.py                 # 图像分块模块
├── super_resolution_module.py       # 超分辨率处理模块
├── blending_module.py               # 图像融合模块
├── quality_assessment_module.py     # 质量评估模块
├── agent_scheduler.py               # Agent集群调度器
│
├── pages/                           # WebUI页面
│   ├── upload_page.py              # 上传与预览
│   ├── config_page.py              # 参数配置
│   ├── monitor_page.py             # 处理监控
│   ├── result_page.py              # 结果展示
│   └── advanced_page.py            # 高级功能
│
├── utils/                           # 工具模块
│   └── session_manager.py          # 会话管理
│
├── styles/                          # 样式模块
│   └── custom_css.py               # 自定义CSS
│
└── README.md                        # 项目文档
```

## 🚀 快速开始

### 1. 环境准备

```bash
# 克隆项目
cd super_resolution_system

# 创建虚拟环境
python -m venv venv
source venv/bin/activate  # Linux/Mac
# 或 venv\Scripts\activate  # Windows

# 安装依赖
pip install -r requirements.txt
```

### 2. 配置API密钥

```bash
# 方式1: 环境变量
export VOLC_AK="your_access_key"
export VOLC_SK="your_secret_key"

# 方式2: 配置文件
cp config.example.py config.py
# 编辑 config.py 填入你的API密钥
```

### 3. 运行Web界面

```bash
streamlit run app.py
```

访问 http://localhost:8501

### 4. 命令行使用

```python
import asyncio
from main import SuperResolutionPipeline, PipelineConfig

async def main():
    config = PipelineConfig(
        volc_ak="your_access_key",
        volc_sk="your_secret_key",
        target_resolution="100MP",
        seedream_strength=0.5
    )
    
    async with SuperResolutionPipeline(config) as pipeline:
        result = await pipeline.process(
            input_path="input.jpg",
            output_path="output.tiff",
            prompt="高端商业摄影，8K超高清，专业广告品质"
        )
        
        if result.success:
            print(f"✅ 处理成功! 耗时: {result.processing_time:.2f}秒")
            print(f"   质量评分: {result.quality_score:.2f}/100")

asyncio.run(main())
```

## ⚙️ 配置说明

### 分块配置 (TilingConfig)

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `block_size` | 2048 | 输入块尺寸（像素） |
| `overlap_ratio` | 0.2 | 重叠率（10%-30%） |
| `padding_mode` | 'mirror' | 边缘填充模式 |

### 超分配置 (SuperResolutionConfig)

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `target_resolution` | '100MP' | 目标分辨率 |
| `seedream_strength` | 0.5 | 生成强度（0.0-1.0） |
| `seedream_steps` | 50 | 推理步数 |

### 调度配置 (SchedulerConfig)

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `max_agents` | 100 | 最大Agent数量 |
| `max_concurrent` | 60 | 最大并发数 |

## 📊 性能指标

| 目标分辨率 | 块布局 | 总块数 | 预估时间(60并发) |
|------------|--------|--------|------------------|
| 100MP | 3×3 | ~16块 | 5-8分钟 |
| 150MP | 4×4 | ~25块 | 8-12分钟 |
| 200MP | 5×5 | ~36块 | 12-18分钟 |

## 🎯 质量评估指标

### 全参考指标

| 指标 | 优良阈值 | 说明 |
|------|----------|------|
| PSNR | >35dB | 峰值信噪比 |
| SSIM | >0.98 | 结构相似性 |
| LPIPS | <0.05 | 感知相似度 |

### 无参考指标

| 指标 | 优良阈值 | 说明 |
|------|----------|------|
| NIQE | <3.0 | 自然图像质量 |
| BRISQUE | <20.0 | 盲图像质量 |

## 🛠️ 开发路线图

| 阶段 | 周期 | 核心交付 |
|------|------|----------|
| MVP | 4-6周 | 固定网格分块、单线程、基础融合 |
| 优化版 | 6-8周 | 拉普拉斯融合、质量评估、Agent集成 |
| 生产版 | 8-12周 | 内容感知分块、批量处理、SLA保障 |

## 📚 技术文档

- [图像分块模块文档](README_tiling.md)
- [超分辨率模块文档](README_super_resolution.md)
- [融合模块文档](README_blending.md)
- [质量评估模块文档](README_QualityAssessment.md)

## ⚠️ 注意事项

1. **API配额**: Seedream 4.0 API调用需要提前申请配额
2. **成本控制**: 建议设置预算硬上限，避免意外超支
3. **内存管理**: 超大图像处理需要足够的系统内存
4. **网络稳定**: 确保稳定的网络连接，避免API调用失败

## 🔒 安全提示

- API密钥请妥善保管，不要提交到版本控制
- 建议使用环境变量或KMS服务管理密钥
- 生产环境请启用HTTPS和访问控制

## 📄 许可证

MIT License

## 🤝 贡献

欢迎提交Issue和Pull Request！

## 📧 联系方式

如有问题，请通过GitHub Issue联系。

---

<p align="center">
  Made with ❤️ by AI Assistant
</p>
