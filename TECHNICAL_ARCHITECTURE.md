# 超分辨率图像生成系统 - 技术架构文档

> 版本: 1.0.0  
> 作者: AI Assistant  
> 创建日期: 2026-02-28

---

## 📋 项目概述

### 目标
实现从720p输入到**1亿-2亿像素**输出的超高分辨率图像生成系统，用于**广告级印刷**。

### 核心挑战
1. **单张图像无法直接生成100MP** - Seedream 4.0限制最大4096x4096
2. **需要分块-并行-融合架构**
3. **接缝消除** - 重叠区域智能融合
4. **风格一致性** - 多块生成保持统一风格
5. **细节重建** - 不只是放大，而是真正的超分辨率

---

## 🏗️ 系统架构

```
┌─────────────────────────────────────────────────────────────┐
│                    超高分辨率图像生成系统                      │
├─────────────────────────────────────────────────────────────┤
│  Input (720p-1080p)                                        │
│       ↓                                                    │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  Phase 1: 图像分块 (TilingModule)                    │   │
│  │  - 内容感知分块                                      │   │
│  │  - 重叠区域计算 (默认20%)                            │   │
│  │  - 边界填充 (mirror/replicate/reflect)              │   │
│  └─────────────────────────────────────────────────────┘   │
│       ↓                                                    │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  Phase 2: 并行超分 (SuperResolutionModule)           │   │
│  │  - Seedream 4.0 API (AI生成)                        │   │
│  │  - veImageX (CNN快速超分)                           │   │
│  │  - 混合策略 (多级串联)                               │   │
│  └─────────────────────────────────────────────────────┘   │
│       ↓                                                    │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  Phase 3: 智能融合 (BlendingModule)                  │   │
│  │  - 拉普拉斯金字塔融合                                │   │
│  │  - 泊松融合                                         │   │
│  │  - 加权渐变融合                                      │   │
│  └─────────────────────────────────────────────────────┘   │
│       ↓                                                    │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  Phase 4: 质量评估 (QualityAssessmentModule)         │   │
│  │  - PSNR/SSIM/LPIPS                                  │   │
│  │  - NIQE/BRISQUE (无参考)                            │   │
│  └─────────────────────────────────────────────────────┘   │
│       ↓                                                    │
│  Output (100MP-200MP TIFF)                                 │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔧 模块详解

### 1. TilingModule (图像分块模块)

**文件**: `tiling_module.py`

**核心功能**:
- 重叠分块策略（支持15%-25%可调重叠率）
- 分块坐标映射系统和元数据管理
- 内容感知分块（保护人脸/文字/产品主体）
- 三级缓存系统（L1内存/L2磁盘/L3对象存储）

**关键类**:
```python
@dataclass
class TileMetadata:
    block_id: str          # 唯一标识符（UUID）
    global_x: int          # 全局坐标系中的X位置
    global_y: int          # 全局坐标系中的Y位置
    input_w: int           # 输入块宽度
    input_h: int           # 输入块高度
    output_w: int          # 输出块宽度（Seedream限制4096）
    output_h: int          # 输出块高度
    overlap_top: int       # 顶部重叠像素数
    overlap_bottom: int    # 底部重叠像素数
    overlap_left: int      # 左侧重叠像素数
    overlap_right: int     # 右侧重叠像素数
```

**关键方法**:
```python
def split_image(self, image_path: str) -> List[Tile]:
    """将图像分割为多个块"""
    # 1. 加载图像 (OpenCV BGR格式)
    # 2. 内容感知分析 (检测人脸/文字)
    # 3. 计算分块位置 (考虑重叠)
    # 4. 提取每个块并填充到标准大小
    # 5. 创建Tile对象并返回列表
```

**当前问题** (已修复):
- [x] global_x/global_y 未设置 (导致所有块粘贴到0,0)
- [x] BGR到RGB颜色转换缺失

---

### 2. SuperResolutionModule (超分辨率模块)

**文件**: `super_resolution_module.py`

**核心功能**:
- Seedream 4.0 API集成（基于扩散模型的智能超分）
- veImageX备选方案（轻量化CNN快速超分）
- Prompt模板系统（行业场景化提示词）
- 重试机制（指数退避）

**支持的提供商**:
```python
class UpscaleProvider(Enum):
    SEEDREAM = "seedream"    # AI生成，效果最好
    VEIMAGEX = "veimagex"    # CNN超分，速度快
    HYBRID = "hybrid"        # 多级串联
```

**Seedream 4.0 API调用**:
```python
async def upscale_seedream(
    self,
    image: Union[str, Image.Image],
    prompt: str,
    strength: float = 0.5,          # 生成强度 0.0-1.0
    size: str = "4096x4096",        # 输出尺寸
    num_inference_steps: int = 50,  # 推理步数
    seed: Optional[int] = None      # 随机种子（风格一致性）
) -> SuperResolutionResult
```

**Prompt模板系统**:
```python
prompt_templates = {
    'beauty': {
        'subject': '高端化妆品商业摄影，柔光棚拍',
        'style': '8K超高清，细腻肤质，专业广告品质，无噪点',
        'quality': '锐利边缘，精确色彩还原，印刷级精度',
        'negative': '模糊，变形，多余元素，色彩偏移，压缩伪影'
    },
    '3c': {
        'subject': '精密数码产品摄影，科技感十足',
        'style': '金属光泽，精密工艺，未来感设计',
        ...
    }
}
```

**当前问题**:
- [ ] API认证失败 (401 Unauthorized)
- [ ] 需要有效的火山引擎API Key

---

### 3. BlendingModule (图像融合模块)

**文件**: `blending_module.py`

**核心功能**:
- 拉普拉斯金字塔融合（推荐，效果最佳）
- 泊松融合（接缝消除）
- 加权渐变融合（快速）

**融合算法对比**:

| 算法 | 优点 | 缺点 | 适用场景 |
|------|------|------|---------|
| Laplacian | 无缝过渡，保留细节 | 计算量大 | 高质量输出 |
| Poisson | 颜色一致性 | 需要mask | 特定区域融合 |
| Weighted | 快速 | 可能有接缝 | 实时预览 |

**拉普拉斯金字塔融合**:
```python
def laplacian_blend(
    self,
    tiles: List[TileInfo],
    output_size: Tuple[int, int]
) -> Image.Image:
    """
    1. 为每个块创建高斯金字塔
    2. 计算拉普拉斯金字塔（差分）
    3. 在每一层融合（加权平均）
    4. 重建图像
    """
```

---

### 4. QualityAssessmentModule (质量评估模块)

**文件**: `quality_assessment_module.py`

**评估指标**:

**全参考指标**（需要原图）:
- PSNR (Peak Signal-to-Noise Ratio) > 35dB 为优良
- SSIM (Structural Similarity) > 0.98 为优良
- LPIPS (Learned Perceptual Similarity) < 0.05 为优良

**无参考指标**（不需要原图）:
- NIQE (Natural Image Quality Evaluator) < 3.0 为优良
- BRISQUE (Blind/Referenceless Image Spatial Quality) < 20.0 为优良

---

## 🔄 Pipeline执行流程

### 标准执行流程

```python
async with SuperResolutionPipeline(config) as pipeline:
    result = await pipeline.process(
        input_path="input.jpg",
        output_path="output.tiff",
        prompt="高端商业摄影，8K超高清，专业广告品质"
    )
```

### 详细步骤

1. **初始化**
   ```
   加载配置 → 初始化各模块 → 验证API连接
   ```

2. **图像分块**
   ```
   加载图像 → 内容分析 → 计算分块位置 → 提取块 → 添加元数据
   ```

3. **并行超分**
   ```
   分发任务到Agent池 → 每块调用Seedream API → 收集结果 → 错误重试
   ```

4. **智能融合**
   ```
   读取所有超分块 → 构建拉普拉斯金字塔 → 逐层融合 → 重建图像
   ```

5. **质量评估**
   ```
   计算PSNR/SSIM → 生成质量报告 → 保存结果
   ```

6. **输出**
   ```
   保存TIFF → 保存预览图 → 记录日志
   ```

---

## 🔌 API集成

### 火山引擎 API配置

**Seedream 4.0**:
```python
endpoint: "https://ark.cn-beijing.volces.com/api/v3/images/generations"
model: "ep-20260228221135-66v8k"
auth: Bearer Token (ARK_API_KEY)
```

**请求格式**:
```json
{
  "model": "ep-20260228221135-66v8k",
  "prompt": "高端产品摄影...",
  "size": "4096x4096",
  "stream": false,
  "watermark": false
}
```

**响应格式**:
```json
{
  "data": [{
    "url": "https://...",
    "revised_prompt": "..."
  }]
}
```

---

## ⚙️ 配置参数

### TilingConfig (分块配置)
```python
block_size: int = 2048        # 输入块尺寸
overlap_ratio: float = 0.2    # 重叠率 (10%-30%)
padding_mode: str = 'mirror'  # 边缘填充模式
```

### SuperResolutionConfig (超分配置)
```python
target_resolution: str = '100MP'  # 100MP, 150MP, 200MP
seedream_strength: float = 0.5    # 生成强度 0.0-1.0
seedream_steps: int = 50          # 推理步数
```

### SchedulerConfig (调度配置)
```python
max_agents: int = 100        # 最大Agent数量
max_concurrent: int = 60     # 最大并发数
```

---

## 🐛 已知问题与修复

### 已修复
| 问题 | 原因 | 修复方案 |
|------|------|---------|
| 输出只有左上角 | global_x/y未设置 | 添加global_x/y到TileMetadata |
| 颜色通道互换 | BGR未转RGB | 添加cv2.cvtColor转换 |

### 待解决
| 问题 | 优先级 | 方案 |
|------|-------|------|
| API认证失败 | 🔴 高 | 验证API Key格式，检查配额 |
| 接缝明显 | 🟡 中 | 完善拉普拉斯金字塔融合 |
| 细节不足 | 🟡 中 | 集成真正的AI超分 |
| 处理速度慢 | 🟢 低 | 优化算法，使用GPU |

---

## 📁 文件结构

```
super_resolution_system/
├── main.py                          # 主入口程序
├── app.py                           # Streamlit WebUI
├── config.py                        # 系统配置
├── requirements.txt                 # 依赖列表
│
├── tiling_module.py                 # 图像分块模块 ⭐
├── super_resolution_module.py       # 超分辨率处理模块 ⭐
├── blending_module.py               # 图像融合模块 ⭐
├── quality_assessment_module.py     # 质量评估模块
├── agent_scheduler.py               # Agent集群调度器
├── ark_api_module.py                # ARK API模块
│
├── fast_super_resolution_pipeline.py # 快速超分Pipeline
├── test_fixed_pipeline.py           # 修复验证脚本
├── process_donut.py                 # 用户图片处理脚本
│
├── pages/                           # WebUI页面
│   ├── upload_page.py
│   ├── config_page.py
│   └── ...
│
├── utils/                           # 工具模块
├── styles/                          # 样式模块
│
├── output/                          # 输出目录
│   ├── donut_fixed_100mp.tiff
│   └── ...
│
└── README.md                        # 项目文档
```

---

## 🚀 优化方向

### 短期优化
1. **修复API认证** - 确保Seedream API可用
2. **完善融合算法** - 实现真正的拉普拉斯金字塔融合
3. **添加进度显示** - 实时显示处理进度

### 中期优化
1. **集成真实AI超分** - 使用火山引擎Seedream进行细节重建
2. **多级超分策略** - 2x→2x→2x 渐进式放大
3. **风格一致性** - 确定性种子，保持整体风格统一

### 长期优化
1. **GPU加速** - 使用CUDA加速处理
2. **分布式处理** - 多机并行处理超大图像
3. **智能分块** - 基于内容的自适应分块

---

## 📚 参考资料

- Seedream 4.0 API文档: https://www.volcengine.com/docs/
- 拉普拉斯金字塔融合: https://en.wikipedia.org/wiki/Pyramid_(image_processing)
- 超分辨率综述: https://arxiv.org/abs/1902.06068

---

## 📝 更新日志

**v1.0.0 (2026-02-28)**
- 初始版本
- 基础分块-融合架构
- 修复global_x/y和颜色空间问题

---

*本文档由AI Assistant生成，供DeepResearch优化参考*
